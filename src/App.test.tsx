import React from 'react';
import { render, screen } from '@testing-library/react';
import App from './App';

describe('Name of the group', () => {
  it('should ', () => {
    expect(1 + 1).toBe(2)
  });
});